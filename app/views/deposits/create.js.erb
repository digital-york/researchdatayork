$("#<%= raw @dataset_id %>").empty().append("<%= escape_javascript(render partial: 'status', object: @deposit ) %>");
$("#<%= raw @dataset_id %>-notes").empty().append("<%= escape_javascript(render partial: 'notes', object: @deposit ) %>");